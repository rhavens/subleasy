<% if user_signed_in? && current_user.likes?(offer) %>
  <%= link_to "Unlike", offer_like_path(offer), method: :delete, remote: true %>
<% else %>
  <%= link_to "Like", offer_like_path(offer), method: :post, remote: true %>
<% end %>

<%= offer.likes.count %>